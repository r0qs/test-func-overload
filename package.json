{
    "name": "test",
    "version": "0.0.1",
    "description": "test",
    "directories": {
        "test": "test"
    },
    "scripts": {
        "compile": "truffle compile",
        "console": "truffle console",
        "migrate": "truffle migrate --reset --network ganache",
        "test": "truffle test --network ganache",
        "test:bug": "NODE_ENV=bug truffle test --network ganache",
        "ganache-cli": "ganache-cli --deterministic --networkId 5777 --host 127.0.0.1 --port 7545"
    },
    "author": "Rodrigo Q. Saramago",
    "license": "GPLv3",
    "dependencies": {},
    "devDependencies": {
        "@openzeppelin/test-helpers": "^0.5.4",
        "ganache-cli": "^6.9.1",
        "truffle": "^5.1.15"
    }
}